---
import { allTreatments } from "@/lib/data";
---

<section
  class="relative bg-surface-100 z-20 flex flex-col items-center pt-32 pb-20"
>
  <div class="absolute inset-0 z-0">
    <div
      class="absolute inset-0 bg-linear-to-br from-white via-surface-100 to-surface-200 opacity-90"
    >
    </div>
    <div
      class="absolute top-1/4 left-1/4 w-48 h-48 bg-primary/10 rounded-full mix-blend-multiply filter blur-3xl opacity-40 animate-pulse-slow"
    >
    </div>
    <div
      class="absolute bottom-1/3 right-1/3 w-60 h-60 bg-logo/5 rounded-full mix-blend-multiply filter blur-3xl opacity-30 animate-pulse-slow delay-500"
    >
    </div>
    <div
      class="absolute inset-0 bg-cover bg-center opacity-30 pointer-events-none"
      style="background-image: url('/assets/images/hero-bg-dermatologia.png');"
    >
    </div>
  </div>

  <div class="container mx-auto px-6 mb-24 relative z-10">
    <div class="flex flex-col md:flex-row items-center justify-between gap-12">
      <div
        class="flex-1 basis-full md:basis-[55%] space-y-8 text-center md:text-left"
      >
        <p
          class="inline-block px-4 py-2 rounded-full border border-base-strong bg-surface-200 text-ink-muted text-xs font-semibold tracking-widest uppercase"
        >
          Clínica Dermatológica <span class="text-primary-dark">en Lima</span>
        </p>

        <h1 class="text-5xl md:text-7xl font-light text-ink-base leading-[1.1]">
          Expertos en la <span
            class="font-medium text-primary font-serif italic"
            >Salud y Belleza</span
          > de tu Piel
        </h1>

        <p
          class="text-xl text-ink-muted max-w-xl mx-auto md:mx-0 leading-relaxed"
        >
          Ofrecemos tratamientos avanzados y personalizados para cuidar la piel
          que te define, con la ciencia y el arte de la dermatología moderna.
        </p>

        <div class="flex flex-wrap gap-5 justify-center md:justify-start">
          <button
            class="bg-primary hover:bg-primary-dark text-ink-on-primary px-10 py-4 rounded-full font-semibold text-lg shadow-xl shadow-primary/20 transition-all transform hover:-translate-y-1 hover:scale-105 cursor-pointer"
          >
            Agenda tu Consulta
          </button>
          <button
            class="bg-surface-100 border border-base-strong text-ink-base px-10 py-4 rounded-full font-semibold text-lg hover:bg-surface-200 transition-colors cursor-pointer"
          >
            Ver Tratamientos
          </button>
        </div>
      </div>

      <div
        class="flex-1 basis-full md:basis-[40%] hidden md:flex justify-end items-center"
      >
        <div
          class="relative w-full max-w-md aspect-3/4 rounded-4xl bg-surface-200 border border-base-light shadow-2xl transform rotate-2"
        >
          <div
            class="absolute inset-0 bg-linear-to-t from-surface-100/40 via-transparent to-transparent overflow-hidden rounded-4xl"
          >
            <img
              src="/assets/images/hero-dermatologia-focus.png"
              alt="Piel sana y radiante"
              class="absolute inset-0 w-full h-full object-cover -rotate-2 scale-110"
            />
          </div>

          <div
            class="absolute bottom-8 -left-6 p-4 bg-white/90 backdrop-blur-md rounded-2xl shadow-xl flex items-center space-x-3 border border-base-light"
          >
            <div class="bg-primary/20 p-2 rounded-lg">
              <svg
                class="w-6 h-6 text-primary"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <span class="text-ink-base pr-2 text-sm tracking-wide"
              >Tecnología de última generación</span
            >
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="container mx-auto z-10 px-6">
    <div
      class="bg-white/70 backdrop-blur-xl border border-base-light p-3 rounded-2xl shadow-2xl flex flex-col md:flex-row items-center gap-4 mx-4 lg:mx-0"
    >
      <div class="flex items-center gap-3 pl-4 min-w-max hidden md:flex">
        <svg
          class="w-5 h-5 text-primary"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
        </svg>
        <span class="text-ink-muted font-medium text-sm"
          >¿En qué podemos ayudarte?</span
        >
      </div>

      <div class="relative grow w-full">
        <input
          id="search-input"
          type="text"
          placeholder="Buscar tratamientos, síntomas..."
          class="w-full bg-surface-200/50 border-none rounded-xl px-6 py-4 text-ink-base placeholder:text-ink-muted/60 focus:ring-2 focus:ring-primary/20 outline-none transition-all"
        />
        <div
          id="search-results"
          class="absolute top-full left-1/2 -translate-x-1/2 md:translate-0 md:left-0 w-[120%] md:w-full bg-white rounded-2xl mt-2 shadow-2xl border border-base-light z-50 hidden"
        >
          <ul class="p-4 space-y-2"></ul>
        </div>
      </div>

      <button
        id="search-button"
        class="w-full md:w-auto bg-primary hover:bg-primary-dark text-ink-on-primary px-8 py-4 rounded-xl font-medium transition-all flex items-center justify-center gap-2 group cursor-pointer"
      >
        <span>Buscar</span>
        <svg
          class="w-4 h-4 group-hover:translate-x-1 transition-transform"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
        </svg>
      </button>
    </div>

    <div
      class="mt-4 flex flex-wrap items-center gap-4 overflow-x-auto pb-2 no-scrollbar px-6 md:px-8"
    >
      <span
        class="text-xs text-ink-muted font-bold uppercase tracking-wider min-w-max"
        >Tendencias:</span
      >
      {
        ["Láser", "Rejuvenecimiento", "Acné", "Manchas"].map((item: string) => (
          <button class="trend-tag text-xs text-ink-muted cursor-pointer bg-primary-light/20 hover:bg-primary-light/40 min-w-max py-1 px-2 rounded-md transition-colors">
            {item}
          </button>
        ))
      }
    </div>
  </div>
</section>

<script is:inline src="https://cdn.jsdelivr.net/npm/fuse.js@7.0.0"></script>

<script define:vars={{ allTreatments }}>
  const fuseOptions = {
    keys: ["title", "tags", "description"],
    threshold: 0.4,
    includeMatches: true,
  };

  const fuse = new window.Fuse(allTreatments, fuseOptions);

  const input = document.getElementById("search-input");
  const resultsContainer = document.getElementById("search-results");
  const resultsList = resultsContainer.querySelector("ul");
  const searchButton = document.getElementById("search-button");

  function performSearch(query) {
    const cleanQuery = query.trim();
    if (cleanQuery.length >= 2) {
      const results = fuse.search(cleanQuery);
      displayResults(results);
      resultsContainer.classList.remove("hidden");
    } else {
      resultsContainer.classList.add("hidden");
    }
  }

  input.addEventListener("input", (e) => {
    performSearch(e.target.value);
  });

  document.querySelectorAll(".trend-tag").forEach((tag) => {
    tag.addEventListener("click", () => {
      const query = tag.innerText.trim();
      input.value = query;
      performSearch(query);
      input.focus();
    });
  });

  searchButton.addEventListener("click", () => {
    const firstResult = resultsList.querySelector("a");
    if (firstResult) {
      firstResult.click();
    } else {
      performSearch(input.value);
    }
  });

  function displayResults(results) {
    if (results.length > 0) {
      const limit = window.innerWidth < 768 ? 4 : 5;
      resultsList.innerHTML = results
        .slice(0, limit)
        .map(
          (result) => `
        <li class="border-b border-base-light last:border-none">
          <a href="/tratamientos/${result.item.slug}" class="flex items-center gap-4 p-4 hover:bg-surface-100 transition-colors group w-full text-left">
            <div class="hidden sm:flex w-10 h-10 shrink-0 rounded-lg bg-primary/10 items-center justify-center text-primary group-hover:bg-primary group-hover:text-white transition-colors">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
            </div>
            <div class="flex flex-col gap-1 flex-1">
              <div class="flex flex-col-reverse md:flex-row md:items-center justify-between gap-2">
                <span class="font-bold text-ink-base text-sm line-clamp-1">${result.item.title}</span>
                <span class="text-[9px] text-primary w-fit font-bold uppercase tracking-tighter px-2 py-0.5 rounded-md bg-primary/10 shrink-0 italic">${result.item.category || "Médico"}</span>  
              </div>
              <span class="text-[11px] text-ink-muted line-clamp-1 italic">${result.item.description || "Ver detalles del tratamiento"}</span>
            </div>
          </a>
        </li>
      `,
        )
        .join("");
    } else {
      resultsList.innerHTML = `
      <div class="p-6 text-center">
        <p class="text-sm text-ink-muted mb-3">No encontramos ese tratamiento...</p>
        <a href="https://wa.me/51999888777" target="_blank" class="inline-flex text-xs font-bold text-primary uppercase tracking-widest border-b border-primary pb-1">Consultar por WhatsApp</a>
      </div>`;
    }
  }

  document.addEventListener("click", (e) => {
    if (
      !input.contains(e.target) &&
      !resultsContainer.contains(e.target) &&
      !e.target.classList.contains("trend-tag")
    ) {
      resultsContainer.classList.add("hidden");
    }
  });
</script>
