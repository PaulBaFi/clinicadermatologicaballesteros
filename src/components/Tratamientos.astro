---
import { allTreatments } from "@/lib/data";
---

<section class="py-24 bg-surface-100">
  <div class="container mx-auto px-6">
    <div
      class="flex flex-col md:flex-row justify-between items-end mb-16 gap-8"
    >
      <div class="max-w-2xl space-y-4">
        <h2 class="text-primary font-serif italic text-2xl">
          Nuestros Servicios
        </h2>
        <h3 class="text-4xl md:text-5xl font-light text-ink-base leading-tight">
          Soluciones Avanzadas para la <span class="font-bold"
            >Salud de tu Piel</span
          >
        </h3>
      </div>
      <a
        href="/tratamientos"
        class="text-ink-base font-medium border-b-2 border-primary pb-1 hover:text-primary transition-colors inline-flex items-center gap-2 group"
      >
        Ver todos los tratamientos
        <svg
          class="w-5 h-5 group-hover:translate-x-1 transition-transform"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
        </svg>
      </a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {
        allTreatments.slice(0, 3).map((t) => (
          <div
            data-category={t.category}
            class="treatment-card group rounded-4xl bg-surface-100 border border-base-light hover:bg-white hover:border-primary/20 hover:shadow-2xl hover:shadow-primary/10 transition-all duration-500 animate-in fade-in zoom-in-95 overflow-hidden"
          >
            <div class="relative w-full aspect-5/4 overflow-hidden">
              <img
                src={t.image}
                alt={t.title}
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
              />
              <div class="absolute inset-0 bg-linear-to-t from-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity" />

              <div class="absolute top-6 right-6">
                <span class="text-[10px] font-bold uppercase tracking-widest text-primary bg-white/90 backdrop-blur-md px-3 py-1 rounded-full shadow-sm">
                  {t.category}
                </span>
              </div>
            </div>

            <div class="p-8 flex flex-col h-auto">
              <div class="flex justify-between items-start mb-6">
                <div class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center shadow-sm text-primary group-hover:bg-primary group-hover:text-white transition-colors duration-500">
                  <svg
                    class="w-6 h-6"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="1.5"
                      d="M13 10V3L4 14h7v7l9-11h-7z"
                    />
                  </svg>
                </div>
              </div>

              <h3 class="text-2xl font-bold text-ink-base mb-4 group-hover:text-primary transition-colors">
                {t.title}
              </h3>

              <p class="text-ink-muted leading-relaxed mb-8 grow line-clamp-3">
                {t.description}
              </p>

              <div class="space-y-6">
                <div class="flex flex-wrap gap-2">
                  {t.tags.map((tag) => (
                    <span class="text-[10px] font-bold text-ink-muted/50 uppercase tracking-wide">
                      #{tag}
                    </span>
                  ))}
                </div>

                <a
                  href={`/tratamientos/${t.slug}`}
                  class="flex items-center justify-between w-full p-4 bg-white border border-base-light rounded-2xl group/btn hover:border-primary transition-all"
                >
                  <span class="text-sm font-bold text-ink-base">
                    Ver detalles
                  </span>
                  <div class="w-8 h-8 rounded-full bg-surface-200 flex items-center justify-center group-hover/btn:bg-primary group-hover/btn:text-white transition-all">
                    <svg
                      class="w-4 h-4"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M9 5l7 7-7 7"
                      />
                    </svg>
                  </div>
                </a>
              </div>
            </div>
          </div>
        ))
      }
    </div>

    <div
      class="mt-20 p-8 md:p-12 bg-surface-100 rounded-3xl border border-base-light flex flex-col md:flex-row items-center justify-between gap-8"
    >
      <div class="flex flex-col md:flex-row items-start md:items-center gap-6">
        <div
          class="w-16 h-16 bg-white rounded-2xl shadow-sm flex items-center justify-center text-primary"
        >
          <svg
            class="w-8 h-8"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"
            ></path>
          </svg>
        </div>
        <div>
          <h5 class="text-xl font-bold text-ink-base">
            ¿No estás seguro de qué tratamiento necesitas?
          </h5>
          <p class="text-ink-muted">
            Agenda una evaluación con el Dr. Ballesteros para un diagnóstico
            preciso.
          </p>
        </div>
      </div>
      <button
        class="bg-primary text-white px-10 py-4 rounded-full font-bold shadow-lg shadow-primary/20 hover:scale-105 transition-transform whitespace-nowrap"
      >
        Solicitar Evaluación
      </button>
    </div>
  </div>
</section>
